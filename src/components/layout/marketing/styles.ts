/**
 * Shared class fragments for the marketing chrome. Focus rings are the one
 * thing the 1280px mocks cannot show, so they are defined once here and reused
 * by every interactive element in the nav and footer.
 */
export const FOCUS_RING =
  "outline-none focus-visible:ring-3 focus-visible:ring-tm-coral/30 focus-visible:ring-offset-1 focus-visible:ring-offset-card";
